mpdoscuatroMOM <- within(mpdosmilcuatroMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilcincoMOM <- within(mpdosmilcincoMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilseisMOM <- within(mpdosmilseisMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilsieteMOM <- within(mpdosmilsieteMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilochoMOM <- within(mpdosmilochoMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilnueveMOM <- within(mpdosmilnueveMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmildiezMOM <- within(mpdosmildiezMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilonceMOM <- within(mpdosmilonceMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmildoceMOM <- within(mpdosmildoceMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmiltreceMOM <- within(mpdosmiltreceMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilcatorceMOM <- within(mpdosmilcatorceMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdosmilquinceMOM <- within(mpdosmilquinceMOM, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)
mpdoscuatroMOH <- within(mpdosmilcuatroMOH, 
                         {
                           #  Completa nombres estados.
                           Estado <- gsub("M.xico.*", "Mexico", Estado)
                           Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                           Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                           Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                           Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                           Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                         }
)
mpdosmilcincoMOH <- within(mpdosmilcincoMOH, 
                           {
                             #  Completa nombres estados.
                             Estado <- gsub("M.xico.*", "Mexico", Estado)
                             Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                             Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                             Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                             Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                             Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                           }
)
mpdosmilseisMOH <- within(mpdosmilseisMOH, 
                          {
                            #  Completa nombres estados.
                            Estado <- gsub("M.xico.*", "Mexico", Estado)
                            Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                            Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                            Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                            Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                            Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                          }
)
mpdosmilsieteMOH <- within(mpdosmilsieteMOH, 
                           {
                             #  Completa nombres estados.
                             Estado <- gsub("M.xico.*", "Mexico", Estado)
                             Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                             Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                             Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                             Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                             Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                           }
)
mpdosmilochoMOH <- within(mpdosmilochoMOH, 
                          {
                            #  Completa nombres estados.
                            Estado <- gsub("M.xico.*", "Mexico", Estado)
                            Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                            Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                            Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                            Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                            Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                          }
)
mpdosmilnueveMOH <- within(mpdosmilnueveMOH, 
                           {
                             #  Completa nombres estados.
                             Estado <- gsub("M.xico.*", "Mexico", Estado)
                             Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                             Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                             Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                             Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                             Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                           }
)
mpdosmildiezMOH <- within(mpdosmildiezMOH, 
                          {
                            #  Completa nombres estados.
                            Estado <- gsub("M.xico.*", "Mexico", Estado)
                            Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                            Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                            Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                            Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                            Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                          }
)
mpdosmilonceMOH <- within(mpdosmilonceMOH, 
                          {
                            #  Completa nombres estados.
                            Estado <- gsub("M.xico.*", "Mexico", Estado)
                            Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                            Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                            Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                            Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                            Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                          }
)
mpdosmildoceMOH <- within(mpdosmildoceMOH, 
                          {
                            #  Completa nombres estados.
                            Estado <- gsub("M.xico.*", "Mexico", Estado)
                            Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                            Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                            Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                            Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                            Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                          }
)
mpdosmiltreceMOH <- within(mpdosmiltreceMOH, 
                           {
                             #  Completa nombres estados.
                             Estado <- gsub("M.xico.*", "Mexico", Estado)
                             Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                             Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                             Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                             Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                             Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                           }
)
mpdosmilcatorceMOH <- within(mpdosmilcatorceMOH, 
                             {
                               #  Completa nombres estados.
                               Estado <- gsub("M.xico.*", "Mexico", Estado)
                               Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                               Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                               Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                               Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                               Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                             }
)
mpdosmilquinceMOH <- within(mpdosmilquinceMOH, 
                            {
                              #  Completa nombres estados.
                              Estado <- gsub("M.xico.*", "Mexico", Estado)
                              Estado <- gsub("Mich.*n.*", "Michoacan", Estado)
                              Estado <- gsub("Nuevo Le.*n.*", "Nuevo Leon", Estado)
                              Estado <- gsub("Quer.taro.*", "Queretaro", Estado)
                              Estado <- gsub("San Luis Pot.*.*", "San Luis Potosi", Estado)
                              Estado <- gsub("Yuca.*n.*", "Yucatan",Estado)
                            }
)