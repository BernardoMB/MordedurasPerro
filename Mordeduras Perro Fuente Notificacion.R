mpdosmilcuatro <- read.csv("2004mp.csv", 
                       colClasses = "character", 
                       na.strings = "#N/A")


mpdosmilcinco <- read.csv("2005mp.csv", 
                           colClasses = "character", 
                           na.strings = "#N/A")

mpdosmilseis <- read.csv("2006mp.csv", 
                          colClasses = "character", 
                          na.strings = "#N/A")

mpdosmilsiete <- read.csv("2007mp.csv", 
                         colClasses = "character", 
                         na.strings = "#N/A")

mpdosmilocho <- read.csv("2008mp.csv", 
                          colClasses = "character", 
                          na.strings = "#N/A")

mpdosmilnueve <- read.csv("2009mp.csv", 
                         colClasses = "character", 
                         na.strings = "#N/A")

mpdosmildiez <- read.csv("2010mp.csv", 
                          colClasses = "character", 
                          na.strings = "#N/A")

mpdosmilonce <- read.csv("2011mp.csv", 
                         colClasses = "character", 
                         na.strings = "#N/A")

mpdosmildoce <- read.csv("2012mp.csv", 
                         colClasses = "character", 
                         na.strings = "#N/A")

mpdosmiltrece <- read.csv("2013mp.csv", 
                         colClasses = "character", 
                         na.strings = "#N/A")

mpdosmilcatorce <- read.csv("2014mp.csv", 
                          colClasses = "character", 
                          na.strings = "#N/A")

mpdosmilquince <- read.csv("2015mp.csv", 
                            colClasses = "character", 
                            na.strings = "#N/A")








